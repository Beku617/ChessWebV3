cd "c:\Users\User\OneDrive\Desktop\Diploma Code V2\ChessWebV3" && node -e "const fs = require('fs'); const path = require('path'); const ext = ['.ts', '.tsx', '.js', '.jsx', '.css']; let files = 0, lines = 0; const walk = (dir) => { try { fs.readdirSync(dir).forEach(f => { const p = path.join(dir, f); if (['node_modules', '.git', 'dist', 'build', '.next', 'MyApp'].includes(f)) return; const s = fs.statSync(p); if (s.isDirectory()) walk(p); else if (ext.includes(path.extname(f))) { files++; lines += fs.readFileSync(p, 'utf-8').split('\n').length; } }); } catch(e) {} }; walk('.'); console.log('Files:', files, 'Lines:', lines);"